# Local development uses the Vite proxy below, so the browser stays same-origin.
# AWAY_API_PROXY_TARGET=http://localhost:3000

# Set only for a deployed web app, for example:
# VITE_API_URL=https://api.example.com
